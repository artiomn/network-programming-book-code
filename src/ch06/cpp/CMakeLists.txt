cmake_minimum_required(VERSION 3.10)

if (WIN32)
    add_subdirectory("pil")
    #add_subdirectory("enum_protocols")
    add_subdirectory("service_base")
    add_subdirectory("mailslot")
    add_subdirectory("named_pipe")
    add_subdirectory("wping")
endif()
